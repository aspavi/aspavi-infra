# IMPORTANTE: Este fichero NO se commitea a Git (está en .gitignore)
# Crear manualmente en el cluster:
#
# 1. Secret de base de datos:
#
# kubectl create secret generic keycloak-db-secret \
#   --from-literal=username=keycloak_user \
#   --from-literal=password="$(security find-generic-password -a "aspavi" -s "KEYCLOAK_USER_PASSWORD" -w)" \
#   -n rrhh-auth
#
# 2. Secret de admin:
#
# kubectl create secret generic keycloak-secrets \
#   --from-literal=admin-password="$(security find-generic-password -a "aspavi" -s "KEYCLOAK_ADMIN_PASSWORD" -w)" \
#   -n rrhh-auth
