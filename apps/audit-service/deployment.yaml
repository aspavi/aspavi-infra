apiVersion: apps/v1
kind: Deployment
metadata:
  name: audit-service
  namespace: rrhh-system
  labels:
    app: audit-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: audit-service
  template:
    metadata:
      labels:
        app: audit-service
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      containers:
        - name: audit-service
          image: ghcr.io/aspavi/aspavi-audit-service:master-03da37f
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
              name: http
          env:
            - name: DB_URL
              value: "jdbc:postgresql://postgresql.rrhh-data:5432/audit_db"
            - name: DB_USERNAME
              value: "app_user"
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgresql-helm-secrets
                  key: app-password
            - name: SPRING_JPA_HIBERNATE_DDL_AUTO
              value: "validate"
            - name: SPRING_PROFILES_ACTIVE
              value: "aspavi"
            # Multitenancy — nombre alineado con ${KEYCLOAK_BASE_URL} en application-aspavi.yml
            - name: KEYCLOAK_BASE_URL
              value: "https://keycloak.aspavi.com"
            # Client secrets del backend (rrhh-audit-service) por realm.
            # Secret creado manualmente: kubectl create secret generic keycloak-client-secrets ...
            # Ver apps/keycloak/secrets.yaml para instrucciones completas.
            - name: KEYCLOAK_CLIENT_SECRET_TEST
              valueFrom:
                secretKeyRef:
                  name: keycloak-client-secrets
                  key: audit-service-secret-test
            - name: KEYCLOAK_CLIENT_SECRET_ACME
              valueFrom:
                secretKeyRef:
                  name: keycloak-client-secrets
                  key: audit-service-secret-acme
          startupProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 12
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 0
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
---
apiVersion: v1
kind: Service
metadata:
  name: audit-service
  namespace: rrhh-system
  labels:
    app: audit-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: audit-service
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: audit-service
  namespace: rrhh-system
  annotations:
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
    traefik.ingress.kubernetes.io/router.middlewares: rrhh-system-cors-headers@kubernetescrd,rrhh-system-audit-strip-prefix@kubernetescrd
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - api.aspavi.com
      secretName: audit-service-tls
  rules:
    - host: api.aspavi.com
      http:
        paths:
          - path: /audit
            pathType: Prefix
            backend:
              service:
                name: audit-service
                port:
                  number: 80
