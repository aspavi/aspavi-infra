# environments/dev/postgresql/values.yaml
# Overrides de desarrollo — sobreescribe helm/postgresql/values.yaml

primary:
  persistence:
    size: 10Gi

  resources:
    requests:
      cpu: "100m"
      memory: "128Mi"
    limits:
      cpu: "500m"
      memory: "512Mi"
