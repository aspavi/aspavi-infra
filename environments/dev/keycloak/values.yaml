# environments/dev/keycloak/values.yaml
# Overrides de desarrollo

replicaCount: 1

# Single replica: local cache avoids JGroups/DNS clustering issues
cache:
  stack: local

resources:
  requests:
    cpu: "250m"
    memory: "512Mi"
  limits:
    cpu: "1000m"
    memory: "1Gi"
