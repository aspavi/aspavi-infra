# environments/prod/argocd/values.yaml
# Overrides de producción

server:
  replicas: 2

repoServer:
  replicas: 2

applicationSet:
  replicas: 2

resources:
  requests:
    cpu: "200m"
    memory: "256Mi"
  limits:
    cpu: "1000m"
    memory: "1Gi"
