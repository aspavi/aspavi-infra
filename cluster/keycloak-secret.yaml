# IMPORTANTE: Este fichero NO se commitea a Git (está en .gitignore)
# Crear manualmente en el cluster con:
#
# KEYCLOAK_ADMIN_PASS=$(security find-generic-password -a "aspavi" -s "KEYCLOAK_ADMIN_PASSWORD" -w)
# KEYCLOAK_DB_PASS=$(security find-generic-password -a "aspavi" -s "KEYCLOAK_USER_PASSWORD" -w)
#
# kubectl create secret generic keycloak-secrets \
#   --from-literal=admin-password="${KEYCLOAK_ADMIN_PASS}" \
#   --from-literal=db-password="${KEYCLOAK_DB_PASS}" \
#   -n rrhh-auth
